name: self test

on:
  push:
  workflow_dispatch:

jobs:
  with-login:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-15-intel, macos-26-intel] # macos-latest it not ready
        provider: [docker, davidkhala]
    runs-on: ${{matrix.os}}
    steps:
    - uses: actions/checkout@main
    - uses: ./
      with:
        action-provider: ${{matrix.provider}}
        username: davidkhala
        password: dckr_pat_ua37yOmE1Xqy2kUOIZEqul2P5Sc
    - run: docker context ls
  default:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - uses: ./
    - run: docker context ls